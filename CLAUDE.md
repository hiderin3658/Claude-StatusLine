# AI 共通コーディングルール（参照用）

> **注意**: このファイルは参照用です。実際に使用するファイルは以下を参照してください。

---

## ファイルの配置

| 用途 | ファイルパス |
|------|-------------|
| **配布用（インストール先）** | `install-to-home/required/CLAUDE.md` |
| **このプロジェクト固有ルール** | `.claude/CLAUDE.md` |
| **テンプレート類** | `install-to-home/required/templates/` |
| **参照ファイル** | `install-to-home/required/references/` |

---

## インストール手順

```bash
# 必須ファイルをコピー
cp install-to-home/required/CLAUDE.md ~/.claude/
cp -r install-to-home/required/templates ~/.claude/
cp -r install-to-home/required/references ~/.claude/
```

詳細は [README.md](./README.md) を参照してください。

---

## 更新履歴

- 2026-01-09: 参照用ファイルに変更（配布用は `install-to-home/required/` に移動）
